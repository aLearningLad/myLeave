export enum EonboardingCategories {
  WO = "worker",
  AD = "admin",
}

export enum EshopStatus {
  IN_SETUP = "in_setup",
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum EsubscriptionStatus {
  SUBBED = "subscribed",
  ON_TRIAL = "trial",
  NOT_SUBBBED = "unsubscribed",
}
